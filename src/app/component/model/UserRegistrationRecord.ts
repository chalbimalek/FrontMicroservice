// src/app/user-registration-record.model.ts
export interface UserRegistrationRecord {
  username: string;
  email: string;
  password: string;
  firstName: string; // Optional field
  lastName: string;  // Optional field
  // Add any other fields you need
}
